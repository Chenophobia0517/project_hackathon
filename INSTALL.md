# 安装指南（INSTALL）

纯静态 Chrome / Edge 扩展，零依赖、无构建步骤，加载仓库目录即可使用。

## 安装步骤

1. 将仓库克隆或下载到**一个固定的本地目录**（浏览器会长期引用该目录，之后删除或移动会导致扩展失效）
2. 打开扩展管理页：地址栏输入 `chrome://extensions`（Edge 为 `edge://extensions`）
3. 打开右上角 **开发者模式**
4. 点击 **加载已解压的扩展程序**，选择**含 `manifest.json` 与 `src/` 的仓库根目录**——注意不是仓库上层目录
5. 扩展出现在列表中即安装成功

## 快速体验

1. 打开任意普通网页（`chrome://` 等内部页面不注入内容脚本）
2. 用鼠标选中一句话（至少 2 个字符）
3. 选区附近出现「深读」按钮，点击它
4. 右侧打开深读侧栏，顶部显示你选中的那句话
5. 侧栏保持打开时继续选中其他内容，侧栏会自动更新——无需关闭再打开

## 常见问题

| 现象 | 原因与解决 |
|---|---|
| 加载时报错 "Manifest file is missing or unreadable" | 选择的是仓库上层目录。须选择**含 manifest.json 的目录**（本扩展为仓库根） |
| 选中文字后没有出现按钮 | 页面是内部页（chrome:// 等）或扩展刚重载过：刷新页面后重试 |
| 点击「深读」后侧栏没打开 | 点击浏览器工具栏的扩展图标手动打开；若仍无反应请在扩展管理页确认扩展已启用 |
| 修改源码后行为未变化 | 在扩展管理页点击该扩展卡片上的刷新按钮重新加载，并刷新测试页面 |
| 移动或删除了仓库目录 | 扩展失效，需重新放置目录或在扩展管理页重新加载 |
